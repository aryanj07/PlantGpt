__pycache__/
*.pyc
.venv/
.env
*.db
.pytest_cache/

# Local object storage (Phase 3 RAG - uploaded documents, dev/solo-scale only,
# see the Phase 3 plan's object-storage ADR). Real content, never committed.
/data/
